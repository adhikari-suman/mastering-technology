/**
 * Provided for you — you do not write this file.
 *
 * `Traffic.class` is a `Class<Traffic>`, and because every enum satisfies
 * `E extends Enum<E>`, that token is enough to get an array of constants back
 * with no cast anywhere.
 */
enum Traffic {
    RED, AMBER, GREEN
}

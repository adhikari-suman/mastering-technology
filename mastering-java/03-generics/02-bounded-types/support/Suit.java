/**
 * Provided for you — you do not write this file.
 *
 * Every enum silently extends `Enum<Suit>`, which is declared as
 * `Enum<E extends Enum<E>>` — the recursive bound this lesson is about.
 */
enum Suit {
    CLUBS, DIAMONDS, HEARTS, SPADES
}

/**
 * The declaration that ships with the library — which is to say, the one you
 * would write on day one when you don't yet know what it does.
 *
 * It is honest and useless: it says the module exists and nothing more. Your
 * job in solution.ts is to replace this with types that describe reality,
 * without touching the library.
 */
declare const legacyCache: {
  create(options?: unknown): unknown;
};
export = legacyCache;

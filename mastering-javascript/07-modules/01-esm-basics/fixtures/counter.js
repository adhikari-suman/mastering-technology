// A module with mutable state, used to demonstrate live bindings.
export let count = 0;
export function increment() { count += 1; }
export const label = 'counter';
export default 'the default export';
